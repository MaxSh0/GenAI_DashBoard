# --- Core UI ---
streamlit>=1.30.0
streamlit-code-editor    # Редактор кода
streamlit-authenticator  # Авторизация пользователей
pyyaml                   # Обязательно для чтения config/users.yaml (для authenticator)

# --- Data Processing ---
pandas
numpy
openpyxl                 # Чтение/запись .xlsx файлов

# --- Visualization ---
plotly                   # Графики Plotly

# --- Google Auth & Sheets ---
google-auth
google-auth-oauthlib
google-auth-httplib2
gspread                  # Коннектор Google Sheets

# --- AI & LLM ---
openai                   # ChatGPT, DeepSeek, кастомные прокси
google-generativeai      # Gemini

# --- Big Data Connectors ---
ytsaurus-client          # YTsaurus
requests                 # Нужно для работы вашего superset.py (API запросы)

# --- Server Architecture (PostgreSQL) ---
sqlalchemy               # ORM для работы с базой данных (замена JSON)
psycopg2-binary          # Драйвер PostgreSQL (теперь обязателен, а не опционален)

# --- Background Tasks & Storage (Celery + Redis + MinIO) ---
celery                   # Очереди задач для тяжелого ETL
redis                    # Брокер сообщений для Celery
boto3                    # S3-клиент для работы с MinIO (сохранение файлов)

# --- Utilities ---
watchdog                 # Авто-перезагрузка Streamlit
cryptography