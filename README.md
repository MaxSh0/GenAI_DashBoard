# 📊 GenAI Dashboard

![Python Version](https://img.shields.io/badge/python-3.13-blue.svg)
![Streamlit](https://img.shields.io/badge/Streamlit-1.40+-FF4B4B.svg)
![Docker](https://img.shields.io/badge/Docker-Enabled-2496ED.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)

**Умная аналитическая платформа с интеграцией Generative AI.**

Этот проект представляет собой интерактивный дашборд, который объединяет данные из Google Sheets и корпоративных хранилищ (YTsaurus/ClickHouse), позволяя строить графики с помощью естественного языка (Text-to-Chart) и управлять процессами обработки данных (ETL) прямо из браузера.

---

## ✨ Ключевые возможности

* **🤖 AI-Powered Analytics:** Генерация кода графиков (Plotly/Matplotlib) по текстовому запросу. Поддержка OpenAI (GPT-4o), Google Gemini и DeepSeek.
* **🔌 Универсальные коннекторы:**
    * **Google Sheets:** Живая синхронизация с таблицами.
    * **YTsaurus / ClickHouse:** Выполнение YQL/SQL запросов к большим данным.
* **🛠️ Встроенный ETL-редактор:** Написание и тестирование Python-скриптов для обработки данных прямо в интерфейсе.
* **🔐 Безопасность:** Авторизация через Google OAuth 2.0. Ролевая модель доступа.
* **🐳 Docker-Ready:** Полная контейнеризация для быстрого развертывания.

---

## 🚀 Быстрый старт

### Вариант 1: Docker (Рекомендуется)

Самый простой способ запустить приложение в изолированной среде.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/MaxSh0/GenAI_DashBoard.git](https://github.com/MaxSh0/GenAI_DashBoard.git)
    cd GenAI_DashBoard
    ```

2.  **Подготовьте конфигурацию (ВАЖНО!):**
    Docker требует наличия конфигурационных файлов перед запуском, иначе он создаст вместо них папки, и возникнет ошибка.
    
    **macOS / Linux / PowerShell:**
    ```bash
    cp client_secret.example.json client_secret.json
    cp user_token.example.json user_token.json
    ```

    **Windows (CMD):**
    ```cmd
    copy client_secret.example.json client_secret.json
    copy user_token.example.json user_token.json
    ```
    *(Затем отредактируйте `client_secret.json`, вставив свои реальные ключи через интерфейс приложения)*
    *(`user_token.json` подтянется сам при первой авторизации в google)*

3.  **Запустите контейнер:**
    ```bash
    docker compose up --build
    ```

Приложение будет доступно по адресу: [http://localhost:8501](http://localhost:8501)

### Вариант 2: Локальный запуск (Native)

Если вы хотите запускать без Docker, используйте автоматические скрипты установки (они сами создадут виртуальное окружение):

**macOS / Linux:**
```bash
chmod +x run_mac.sh
./run_mac.sh

```

**Windows:**

```cmd
run_windows.bat

```

---

## ⚙️ Настройка интеграций

Для полноценной работы (авторизация Google, доступ к таблицам) нужны API ключи.

1. **Google Cloud Platform:**
* Создайте проект в [Google Cloud Console](https://console.cloud.google.com/).
* Включите API: `Google Sheets API`, `Drive API`.
* Создайте **OAuth 2.0 Client ID** (тип Web Application).
* Скачайте JSON-файл, переименуйте его в `client_secret.json` и замените им заглушку в корне проекта.
* **Важно:** Добавьте `http://localhost:8501` в список *Authorized redirect URIs*.


2. **LLM Провайдеры (Опционально):**
* Ключи API для OpenAI/Gemini/DeepSeek вводятся прямо в интерфейсе приложения (меню настроек ⚙️) и сохраняются локально в зашифрованном виде.



---

## 🏗 Структура проекта

```text
GenAI_DashBoard/
├── app.py                     # 🚀 Точка входа (Main application)
├── modules/                   # 📦 Ядро системы
│   ├── auth.py                # Авторизация Google
│   ├── llm_manager.py         # Интеграция с нейросетями
│   ├── connectors/            # Драйверы (GSheets, YTsaurus)
│   └── wizards.py             # UI-компоненты
├── handlers/                  # 🐍 ETL-скрипты (Python processors)
├── charts/                    # 📊 Сгенерированные графики
├── data/                      # 💾 Локальное хранилище данных (gitignored)
├── config/                    # ⚙️ Конфигурационные файлы
├── client_secret.example.json # 📄 Пример конфига доступа
├── Dockerfile                 # Инструкция сборки образа
└── docker-compose.yml         # Оркестрация контейнеров

```

---

## 🛠 Технологический стек

* **Frontend/Backend:** [Streamlit](https://streamlit.io/)
* **Data Processing:** Pandas, NumPy
* **Visualization:** Plotly Express, Graph Objects
* **Big Data:** YTsaurus Client (YQL)
* **Environment:** Python 3.13, Docker

---

## 🤝 Contributing

Мы приветствуем вклад в развитие проекта!

1. Fork it (`https://github.com/MaxSh0/GenAI_DashBoard/fork`)
2. Create your feature branch (`git checkout -b feature/my-new-feature`)
3. Commit your changes (`git commit -am 'Add some feature'`)
4. Push to the branch (`git push origin feature/my-new-feature`)
5. Create a new Pull Request

---

## 📄 Лицензия

Этот проект распространяется под лицензией [MIT](https://www.google.com/search?q=LICENSE).

---

*Developed with ❤️ by MaxSh0*
