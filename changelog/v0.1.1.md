# Версия 0.1.1 (Beta) - Export & Stability Update

**Дата релиза: в БЕТУ** 07.02.2025

В этом обновлении мы сосредоточились на двух вещах: возможности делиться своей работой (Экспорт/Импорт) и стабильности работы приложения при большом количестве графиков.

## 🚀 Новые возможности

### 📦 Система пакетов .geb (GenAI Ecosystem Bundle)
Теперь графики можно переносить между проектами "как есть" — вместе с данными и настройками.
* **Экспорт:** Добавлена кнопка скачивания `.geb` файла в меню графика. Пакет включает код (`.py`), данные (`.csv`/`.xlsx`) и метаданные.
* **Импорт:** В сайдбар добавлен загрузчик пакетов.
    * **Smart Import:** Система автоматически переименовывает файлы при конфликтах (например, добавляет timestamp).
    * **Auto-Link:** Импортированный график сразу добавляется на текущую открытую страницу дашборда.

## 🛡️ Стабильность и Ядро

### Global Widget Isolation (Monkey Patching)
Решена критическая проблема Streamlit с дублированием ключей (`DuplicateElementKey`).
* Реализован глобальный перехватчик для всех интерактивных виджетов (`st.button`, `st.plotly_chart` и др.).
* Теперь каждому виджету "на лету" присваивается уникальный суффикс.
* **Результат:** Можно создавать копии графиков и удалять их в любом порядке — интерфейс не ломается.

## ✨ Улучшения и Исправления
* **UX:** Скачивание пакета экспорта происходит мгновенно (in-memory) без лишней перезагрузки страницы.
* **Fix:** Добавлен авто-патч для `st.plotly_chart`, если в коде графика забыли указать аргумент `key`.